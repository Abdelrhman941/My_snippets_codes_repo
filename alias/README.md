```md
════════════════════════════════════════════════════════════════════════════
║                        🚀 GIT BASH ALIASES GUIDE                         ║
════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│ 📁 NAVIGATION                                                           │
├─────────────────────────────────────────────────────────────────────────┤
│ ..          → cd ..              (go up one directory)                  │
│ ...         → cd ../..           (go up two directories)                │
│ ....        → cd ../../..        (go up three directories)              │
│ back        → cd -               (go back to previous directory)        │
│ cls         → clear              (clear terminal screen)                │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🐍 CONDA/MAMBA                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│ ca <env>    → mamba activate     (activate environment)                 │
│ cdv         → mamba deactivate   (deactivate current env)               │
│ cel         → mamba env list     (list all environments)                │
│ ci <pkg>    → mamba install -y   (install package)                      │
│ cu <pkg>    → mamba update       (update package)                       │
│ cr <pkg>    → mamba remove       (remove package)                       │
│ cec <file>  → create env from yml (mamba env create -f)                 │
│ cee         → export env to yml  (mamba env export > environment.yml)   │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🔧 GIT                                                                  │
├─────────────────────────────────────────────────────────────────────────┤
│ gst         → git status         (show working tree status)             │
│ gad         → git add .          (add all changes)                      │
│ gaa         → git add --all      (add all including deleted)            │
│ gcm "msg"   → git commit -m      (commit with message)                  │
│ gps         → git push           (push to remote)                       │
│ gpl         → git pull           (pull from remote)                     │
│ gco <br>    → git checkout       (switch branch)                        │
│ gbr         → git branch         (list branches)                        │
│ gdf         → git diff           (show changes)                         │
│ gcl <url>   → git clone          (clone repository)                     │
│ glg         → git log (graph)    (show last 10 commits)                 │
│ ginit       → init + first commit (git init && add && commit)           │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 📦 PYTHON & PIP                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│ py          → python             (run python)                           │
│ pip         → python -m pip      (run pip safely)                       │
│ pipi <pkg>  → pip install        (install package)                      │
│ pipu <pkg>  → pip install --upgrade (upgrade package)                   │
│ pipf        → freeze to requirements.txt (save dependencies)            │
│ pipr        → install from requirements.txt (restore dependencies)      │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 📂 FILE OPERATIONS                                                      │
├─────────────────────────────────────────────────────────────────────────┤
│ ll          → ls -lh             (list with sizes)                      │
│ la          → ls -A              (list all including hidden)            │
│ lla         → ls -lAh            (list all with details)                │
│ sizeof <dir>→ du -sh             (show directory size)                  │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🧠 ML/DS SPECIFIC                                                       │
├─────────────────────────────────────────────────────────────────────────┤
│ pyprof <script> → profile python code (show performance bottlenecks)    │
│ gpus        → nvidia-smi         (show GPU status)                      │
│ gpuw        → watch nvidia-smi   (monitor GPU live)                     │
│ serve       → python http server (serve current dir on :8000)           │
│ tb <dir>    → tensorboard        (launch tensorboard)                   │
│ pytest      → run pytest -v      (run tests verbosely)                  │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🌐 UTILITIES                                                            │
├─────────────────────────────────────────────────────────────────────────┤
│ myip        → curl ifconfig.me   (show public IP)                       │
│ weather     → curl wttr.in       (show weather)                         │
│ ports       → netstat -tuln      (show open ports)                      │
└─────────────────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════════════
║                        ⚡ POWERFUL FUNCTIONS                             ║
════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────┐
│ 🔨 ENVIRONMENT MANAGEMENT                                                │
├──────────────────────────────────────────────────────────────────────────┤
│ mkenv <name> [pyver]                                                     │
│   → Create new environment with specified Python version                 │
│   * Example: mkenv myproject 3.11                                        │
│                                                                          │
│ mlstack                                                                  │
│   → Install essential ML/DS packages                                     │
│   * Installs: numpy, pandas, matplotlib, seaborn, scikit-learn, jupyter  │
│                                                                          │
│ torchinstall                                                             │
│   → Install PyTorch with CUDA 11.8 support                               │
│                                                                          │
│ envsize                                                                  │
│   → Show size of all conda environments                                  │
│   * Helps identify large environments to clean up                        │
│                                                                          │
│ fullexport [name]                                                        │
│   → Export environment to .yml + requirements.txt                        │
│   * Example: fullexport myproject                                        │
└──────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🐍 PYTHON HELPERS                                                       │
├─────────────────────────────────────────────────────────────────────────┤
│ ipy                                                                     │
│   → Launch IPython (if installed in current env)                        │
│                                                                         │
│ jnb                                                                     │
│   → Launch Jupyter Notebook (if installed)                              │
│                                                                         │
│ jlab                                                                    │
│   → Launch Jupyter Lab (if installed)                                   │
│                                                                         │
│ pyclean                                                                 │
│   → Remove all __pycache__ and .pyc files recursively                   │
│   * Cleans up Python cache from current directory                       │
│                                                                         │
│ pylab                                                                   │
│   → Python REPL with pre-imported np, pd, plt, sns                      │
│   * Quick interactive session for data exploration                      │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ ⏱️ TRAINING & PROFILING                                                 │
├─────────────────────────────────────────────────────────────────────────┤
│ train <command>                                                         │
│   → Run command with automatic timing                                   │
│   * Example: train python train.py                                      │
│   * Shows: start time, end time, duration, exit status                  │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🚀 GIT WORKFLOWS                                                        │
├─────────────────────────────────────────────────────────────────────────┤
│ qcm [message]                                                           │
│   → Quick commit: add + commit + push in one command                    │
│   * Example: qcm "Fixed bug in model.py"                                │
│   * If no message: uses auto-generated timestamp                        │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 📊 VISUALIZATION                                                        │
├─────────────────────────────────────────────────────────────────────────┤
│ tree [depth]                                                            │
│   → Show directory tree structure                                       │
│   * Example: tree 3 (show 3 levels deep)                                │
│                                                                         │
│ path                                                                    │
│   → Display PATH variable in readable format                            │
│   * Shows each path on separate line with numbers                       │
└─────────────────────────────────────────────────────────────────────────┘

╔════════════════════════════════════════════════════════════════════════════╗
║                        💡 COMMON WORKFLOWS                                 ║
╔════════════════════════════════════════════════════════════════════════════╗

┌─────────────────────────────────────────────────────────────────────────┐
│ 🎯 Starting a New ML Project                                            │
├─────────────────────────────────────────────────────────────────────────┤
│ 1. mkenv myproject 3.11        # Create environment                     │
│ 2. mlstack                     # Install ML essentials                  │
│ 3. torchinstall                # Install PyTorch (if needed)            │
│ 4. ginit                       # Initialize git repo                    │
│ 5. jlab                        # Launch Jupyter Lab                     │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🔄 Daily Git Workflow                                                   │
├─────────────────────────────────────────────────────────────────────────┤
│ 1. gpl                         # Pull latest changes                    │
│ 2. gst                         # Check status                           │
│ 3. [make your changes]                                                  │
│ 4. qcm "Your commit message"   # Quick commit & push                    │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🧹 Cleanup & Maintenance                                                │
├─────────────────────────────────────────────────────────────────────────┤
│ 1. pyclean                     # Remove Python cache                    │
│ 2. envsize                     # Check environment sizes                │
│ 3. cr <unused_pkg>             # Remove unused packages                 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🚀 Training a Model                                                     │
├─────────────────────────────────────────────────────────────────────────┤
│ 1. ca myproject                # Activate environment                   │
│ 2. gpus                        # Check GPU availability                 │
│ 3. train python train.py       # Run with automatic timing              │
│ 4. tb runs/                    # View tensorboard logs                  │
└─────────────────────────────────────────────────────────────────────────┘

╔════════════════════════════════════════════════════════════════════════════╗
║  📝 To view this guide anytime: cat ~/alias.txt                            ║
║  💾 To edit aliases: nano ~/.bash_profile && source ~/.bash_profile        ║
╚════════════════════════════════════════════════════════════════════════════╝
```
